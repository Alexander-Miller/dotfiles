;;; preload-cfg.el --- settings to be defined before any packages are loaded

;;; Commentary:
;;; Code:

(setq-default
 gc-cons-threshold             1048576
 diredp-ignore-compressed-flag nil)

(provide 'preload-cfg)
;;; preload-cfg.el ends here
